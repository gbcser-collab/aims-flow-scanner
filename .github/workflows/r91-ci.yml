name: AIMS Flow R91/R92 CI

on:
  push:
    branches:
      - aims-flow-driver-v1-r91-invoice-scanner
      - aims-flow-driver-v1-r92-driver-first-all-in-one
      - aims-flow-r93-stability-hardening
      - aims-flow-r94-full-system-hardening
  workflow_dispatch:

concurrency:
  group: aims-flow-r91-r92-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Flutter stable
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: PHP CLI
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y php-cli php-sqlite3

      - name: Generate Android platform
        run: |
          rm -rf /tmp/aims_template
          flutter create --platforms=android --org hu.logisticaims --project-name aims_flow_scanner /tmp/aims_template
          cp -R /tmp/aims_template/android ./android

      - name: Configure Android
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import shutil

          p = Path('android/app/src/main/AndroidManifest.xml')
          s = p.read_text()
          permissions = [
              'android.permission.CAMERA',
              'android.permission.INTERNET',
              'android.permission.ACCESS_COARSE_LOCATION',
              'android.permission.ACCESS_FINE_LOCATION',
              'android.permission.ACCESS_BACKGROUND_LOCATION',
              'android.permission.FOREGROUND_SERVICE',
              'android.permission.FOREGROUND_SERVICE_LOCATION',
              'android.permission.FOREGROUND_SERVICE_MICROPHONE',
              'android.permission.RECORD_AUDIO',
              'android.permission.WAKE_LOCK',
              'android.permission.USE_BIOMETRIC',
              'android.permission.POST_NOTIFICATIONS',
              'android.permission.VIBRATE',
          ]
          block = '\n'.join(f'    <uses-permission android:name="{name}" />' for name in permissions)
          queries = '\n'.join([
              '    <queries>',
              '        <intent><action android:name="android.speech.RecognitionService" /></intent>',
              '        <intent><action android:name="android.intent.action.TTS_SERVICE" /></intent>',
              '    </queries>',
          ])
          s = s.replace('<application', block + '\n' + queries + '\n    <application', 1)
          s = s.replace('android:label="aims_flow_scanner"', 'android:label="AIMS Flow"')
          assist = '\n'.join([
              '            <intent-filter>',
              '                <action android:name="android.intent.action.ASSIST" />',
              '                <category android:name="android.intent.category.DEFAULT" />',
              '            </intent-filter>',
              '',
          ])
          end = s.find('</activity>')
          if end < 0:
              raise SystemExit('MainActivity closing tag missing')
          s = s[:end] + assist + s[end:]
          service = '\n'.join([
              '        <service',
              '            android:name=".AimsHandsFreeService"',
              '            android:enabled="true"',
              '            android:exported="false"',
              '            android:foregroundServiceType="microphone" />',
              '',
          ])
          s = s.replace('</application>', service + '    </application>', 1)
          p.write_text(s)

          target = Path('android/app/src/main/kotlin/hu/logisticaims/aims_flow_scanner')
          target.mkdir(parents=True, exist_ok=True)
          shutil.copy2('native/android/MainActivity.kt', target / 'MainActivity.kt')
          shutil.copy2('native/android/AimsHandsFreeService.kt', target / 'AimsHandsFreeService.kt')

          g = Path('android/app/build.gradle.kts')
          text = g.read_text()
          text = text.replace('minSdk = flutter.minSdkVersion', 'minSdk = 23')
          if 'isCoreLibraryDesugaringEnabled = true' not in text:
              text = text.replace('compileOptions {', 'compileOptions {\n        isCoreLibraryDesugaringEnabled = true', 1)
          if 'coreLibraryDesugaring(' not in text:
              if 'dependencies {' in text:
                  text = text.replace('dependencies {', 'dependencies {\n    coreLibraryDesugaring("com.android.tools:desugar_jdk_libs:2.1.5")', 1)
              else:
                  text += '\n\ndependencies {\n    coreLibraryDesugaring("com.android.tools:desugar_jdk_libs:2.1.5")\n}\n'
          g.write_text(text)
          PY

          mkdir -p android/app/src/main/res/raw
          base64 -d native/android/raw/aims_new_job.mp3.b64 > android/app/src/main/res/raw/aims_new_job.mp3
          base64 -d native/android/raw/aims_new_message.mp3.b64 > android/app/src/main/res/raw/aims_new_message.mp3
          test -s android/app/src/main/res/raw/aims_new_job.mp3
          test -s android/app/src/main/res/raw/aims_new_message.mp3

      - name: Dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      # Run the crash-specific gate before the broad suite so an R93
      # regression is visible independently from unrelated legacy tests.
      - name: R93 signal/message source guard
        run: python3 ci/r93_signal_message_source_guard.py

      - name: R93 signal and message crash regression
        run: flutter test test/r93_signal_message_regression_test.dart --reporter expanded

      - name: R94 cross-channel isolation guard
        run: python3 ci/r94_cross_channel_isolation_guard.py

      - name: R94 response contract guard
        run: python3 ci/r94_response_contract_guard.py

      - name: R94 document gate guard
        run: python3 ci/r94_document_gate_guard.py

      - name: R94 push lifecycle guard
        run: python3 ci/r94_push_lifecycle_guard.py

      - name: R94 tracking contract guard
        run: python3 ci/r94_tracking_contract_guard.py

      - name: R94 offline state quarantine guard
        run: python3 ci/r94_offline_state_guard.py

      - name: Unit tests
        run: flutter test --reporter expanded

      - name: PHP syntax
        run: |
          find server/aims-tracking admin api -type f -name '*.php' -print0 | xargs -0 -n1 php -l

      - name: R94 backend idempotency E2E
        run: python3 ci/r94_backend_idempotency_test.py

      - name: Job waiting rules
        run: php server/aims-tracking/test_job_waiting.php

      - name: Geocode fallback rules
        run: php server/aims-tracking/test_geocode_candidates.php

      - name: Document gate lifecycle E2E
        run: python3 ci/r92_document_gate_test.py

      - name: Runtime configuration contract
        env:
          AIMS_TRACKING_TOKEN: ${{ secrets.AIMS_TRACKING_TOKEN }}
        run: |
          if [ -z "${AIMS_TRACKING_TOKEN}" ]; then
            echo "::error::AIMS_TRACKING_TOKEN is missing; refusing to build a runtime-broken APK."
            exit 1
          fi
          if [ "${#AIMS_TRACKING_TOKEN}" -lt 16 ]; then
            echo "::error::AIMS_TRACKING_TOKEN is unexpectedly short; refusing to build."
            exit 1
          fi
          echo "AIMS runtime tracking configuration: PASS"

      - name: Build installable R94 APK
        env:
          AIMS_TRACKING_TOKEN: ${{ secrets.AIMS_TRACKING_TOKEN }}
        run: |
          flutter build apk --debug \
            --dart-define=AIMS_TRACKING_BASE_URL=https://logistic-aims.hu/api/aims-tracking \
            --dart-define=AIMS_TRACKING_TOKEN="${AIMS_TRACKING_TOKEN}"

      - name: APK integrity
        run: |
          test -s build/app/outputs/flutter-apk/app-debug.apk
          unzip -t build/app/outputs/flutter-apk/app-debug.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: AIMS-Flow-${{ github.ref_name }}-ANDROID
          path: build/app/outputs/flutter-apk/app-debug.apk
          if-no-files-found: error
